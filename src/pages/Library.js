import { Shell } from "../components/Shell.js";
import { getFavorites,getWrongs,getVocabulary } from "../services/learning.js";
export function Library(kind){const items=kind==='favorites'?getFavorites():getWrongs();const title=kind==='favorites'?'⭐ Favorites':'❌ Wrong notes';return Shell(`<main class="page"><button class="back" onclick="goPage('dashboard')">← Dashboard</button><h1>${title}</h1>${items.length?items.map(x=>`<section class="card"><span class="badge">${x.module.toUpperCase()}</span><h3>${x.question||x.title||''}</h3><p>${x.explanation||''}</p>${x.wrongCount?`<small>Wrong ${x.wrongCount} times</small>`:''}</section>`).join(''):'<div class="card">Nothing here yet.</div>'}</main>`)}
export function Vocabulary(){const words=getVocabulary();return Shell(`<main class="page"><button class="back" onclick="goPage('dashboard')">← Dashboard</button><h1>📚 Vocabulary</h1><section class="card"><p>${words.length} saved words from favorites and wrong notes.</p><div class="chips">${words.map(w=>`<span>${w}</span>`).join('')}</div></section></main>`)}
