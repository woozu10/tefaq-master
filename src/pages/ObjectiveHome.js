import { Shell } from "../components/Shell.js";
import { getBank,getSession,startSession } from "../services/objectiveQuiz.js";
export function ObjectiveHome(module){const name=module==='co'?'Compréhension orale':'Compréhension écrite'; const s=getSession(module); window.startObjective=(m,mode)=>{startSession(m,mode);goPage(`${m}-quiz`)}; return Shell(`<main class="page"><button class="back" onclick="goPage('dashboard')">← Dashboard</button><h1>${module==='co'?'🎧':'📖'} ${name}</h1><div class="card"><p>${getBank(module).length} questions available.</p>${s?`<button onclick="goPage('${module}-quiz')">▶ Continue ${s.index+1}/${s.questions.length}</button>`:''}<button onclick="startObjective('${module}','practice')">Practice</button><button onclick="startObjective('${module}','exam')">Exam</button></div></main>`)}
